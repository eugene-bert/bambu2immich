services:
  bambu2immich:
    image: ghcr.io/eugene-bert/bambu2immich:latest
    container_name: bambu2immich
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data/timelapses
    networks:
      - immich_default

networks:
  immich_default:
    external: true
