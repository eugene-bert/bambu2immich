# Bambu Lab printer
BAMBU_IP=192.168.x.x
BAMBU_ACCESS_CODE=your_access_code
BAMBU_SERIAL=your_printer_serial
# Optional display name used in Immich description and local filenames (default: bambu-a1)
# BAMBU_NAME=bambu-a1

# Immich
# If this container shares Immich's Docker network, use the Immich service name:
IMMICH_URL=http://immich_server:2283
# Otherwise use the published host port:
# IMMICH_URL=http://host.docker.internal:2283
IMMICH_API_KEY=your_immich_api_key

# Optional: Telegram notifications
# TELEGRAM_BOT_TOKEN=your_bot_token
# TELEGRAM_CHAT_ID=your_chat_id

# Optional: keep local copies after upload (default: false)
# KEEP_LOCAL=false

# Optional: download directory (default: /data/timelapses)
# DOWNLOAD_DIR=/data/timelapses
